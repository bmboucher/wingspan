"""Placeholder: SetupDataStore was removed (offline JSONL store eliminated).

The setup model is now always actor-critic, trained on in-memory SetupSample
objects collected during self-play. There is no on-disk sample store.
"""
